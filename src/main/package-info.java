/**
 * MarkdownExcel is split into two packages. The package main.api contains
 * the interfaces describing the API of this tool. The package main.markdownExcel
 * contains the implementation of these interfaces and therefore the implementation
 * of the API itself.
 *
 * @see main.api
 * @see main.markdownExcel
 */
package main;