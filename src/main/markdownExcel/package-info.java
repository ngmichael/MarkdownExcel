/**
 * Default implementation of the MarkdownExcel-API. This is an implementation package.
 * Since MarkdownExcel is intended to be used as a library, there isn't a 'main()-Method'
 * in any class, but there is a Main class. The main class is MarkdownTableBuilder. All most
 * every feature of MarkdownExcel is available through this class.
 *
 * @see main.markdownExcel.MarkdownTableBuilder
 */
package main.markdownExcel;